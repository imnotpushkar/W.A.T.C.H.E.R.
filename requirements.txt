# ============================================================
# WATCHER — Python Dependencies
# ============================================================
# Install: pip install -r requirements.txt
#
# PYTHON VERSION: 3.12+ required. Tested on 3.14.
# COST: All free. edge-tts uses Microsoft's free TTS service (needs internet).
# ============================================================

# --- Core ---
python-dotenv>=1.0.1        # Reads .env into os.environ

# --- LLM ---
requests>=2.31.0            # HTTP client for Ollama REST API
httpx>=0.27.0               # Async HTTP for streaming

# --- Screen Reading (Windows only) ---
uiautomation>=2.0.18        # Windows Accessibility API
comtypes>=1.2.1             # Required by uiautomation

# --- Keyboard / Mouse ---
pynput>=1.7.6               # Global hotkey detection
pyautogui>=0.9.54           # Types text on user's behalf
pyperclip>=1.8.0            # Clipboard access (used by typist fallback)

# --- Voice Output ---
edge-tts>=6.1.9             # Microsoft neural TTS (free, needs internet)
playsound3>=1.0.0           # Audio playback — no C compilation needed

# --- UI ---
PySide6>=6.6.0              # Qt6 Python bindings — replaces PyQt5
                            # PyQt5 does not support Python 3.12+

# --- Voice Input (Phase 2) ---
# faster-whisper>=1.0.1     # Local speech-to-text
# vosk>=0.3.45              # Wake word detection

# --- Memory (Phase 2) ---
# chromadb>=0.5.0           # Vector store for semantic memory

# --- Utilities ---
Pillow>=10.0.0              # Image handling for pyautogui

# --- kokoro-onnx (local TTS — primary engine) ---
kokoro-onnx>=0.4.7          # Local neural TTS, Python 3.14 compatible
soundfile>=0.13.0           # Writes WAV files from kokoro audio arrays
onnxruntime>=1.20.1         # ONNX model inference runtime
espeakng-loader>=0.2.4      # Bundles espeak-ng for phonemization (no manual install)
