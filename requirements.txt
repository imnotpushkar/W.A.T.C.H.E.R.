# ============================================================
# WATCHER — Python Dependencies
# ============================================================
# Install all: pip install -r requirements.txt
#
# COST NOTE: Every package here is free and open source.
# edge-tts makes network calls to Microsoft's TTS service
# (free, no account needed, no rate limits documented).
# All other packages are fully local/offline.
# ============================================================

# --- Core / Config ---
python-dotenv==1.0.1        # Reads .env file into os.environ

# --- LLM (Local AI Brain) ---
requests==2.31.0            # HTTP client for talking to Ollama's REST API
httpx==0.27.0               # Async HTTP client (used for streaming responses)

# --- Screen Reading ---
# uiautomation is Windows-only. Install separately:
# pip install uiautomation
# Listed here for documentation; may need manual install on some setups.
uiautomation==2.0.18        # Windows Accessibility API wrapper

# --- Keyboard / Mouse ---
pynput==1.7.6               # Global keyboard hook and hotkey detection
pyautogui==0.9.54           # Types text and controls mouse on your behalf

# --- Voice Output ---
edge-tts==6.1.9             # Microsoft neural TTS (free, high quality)
pygame==2.5.2               # Audio playback for TTS output files
# pyttsx3==2.90             # Offline TTS fallback (uncomment if needed)

# --- Voice Input (Phase 2 — install when needed) ---
# faster-whisper==1.0.1     # Local speech-to-text
# vosk==0.3.45              # Wake word / offline STT alternative

# --- UI (Overlay, Tray, Settings) ---
PyQt5==5.15.10              # Always-on-top transparent overlay window

# --- Memory / Storage (Phase 2 — install when needed) ---
# chromadb==0.5.0           # Local vector store for semantic memory search

# --- Utilities ---
Pillow==10.3.0              # Image handling (used by pyautogui for screenshots)
